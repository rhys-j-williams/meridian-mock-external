{
  "name": "@meridian/semaphore-client",
  "version": "1.3.2",
  "description": "Minimal Semaphore feature flag client: fetch, cache, and offline evaluation with the same algorithm as the server",
  "license": "UNLICENSED",
  "private": false,
  "publishConfig": {
    "registry": "http://localhost:4873"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build"
  }
}
