{
  "name": "@meridian/paylink-network-mock",
  "version": "2.0.1",
  "private": true,
  "description": "PayLink person to person network mock: contact lookup, send with idempotency, request money, settlement webhook",
  "license": "UNLICENSED",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "jest --runInBand --rootDir . --config ../jest.config.js"
  },
  "dependencies": {
    "express": "4.18.2"
  },
  "devDependencies": {
    "@types/express": "4.17.21"
  }
}
