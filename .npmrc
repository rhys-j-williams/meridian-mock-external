# Mocks resolve from the public registry directly; they are the thing that brings Verdaccio up, so
# they cannot depend on it. Every other directory in the estate points at localhost:4873.
registry=https://registry.npmjs.org/
save-exact=true
package-lock=true
fund=false
audit=false
